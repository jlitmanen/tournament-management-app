<div>
    <table class="table table-dark table-bordered table-hover table-responsive-sm">
        <thead>
            <tr>
                <th>Otsikko</th>
                <th><form id="about" action="/admin/about/add" method="post">
                        <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                        <input type="submit" value="Add"/>
                    </form>
                </th>
            </tr>
        </thead>
        <tbody>
            <% content?.forEach(function(c) { %>
                <tr>
                    <td><%- c.title %></td>
                    <td>
                        <form method="POST" action="/admin/about/edit">
                            <input type="hidden" name="id" id="id" value="<%= c.id %>"/>
                            <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                            <input type="submit" value="Edit" class="edit">
                        </form>
                    </td>
                </tr>

            <% }) %>
        </tbody>
    </table>
</div>
